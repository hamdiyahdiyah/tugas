program  mahasiswa;
 uses crt;

 var
    nama : integer;
    keaktifan : real;
    tugas : char;
    ujian : string;
 begin
    clrscr; 
     {input nama}  
     write ('masukkan nama :');
     readln (nama);

     write ('masukkan nilai keaktifan :');
     readln (keaktifan);

     write ('masukkan nilai tugas :');
     readln (tugas);

     write ('masukkan nilai ujian :');
     readln (ujian);

     {output data}
     writeln()

end.     
